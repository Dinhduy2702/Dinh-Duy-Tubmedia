import type { LucideIcon } from 'lucide-react';
export function StatCard({title,value,subtitle,icon:Icon}:{title:string;value:string|number;subtitle?:string;icon:LucideIcon}){return <div className="card p-4"><div className="flex items-start justify-between"><div><div className="text-xs font-bold uppercase tracking-wide" style={{color:'var(--muted)'}}>{title}</div><div className="mt-2 text-2xl font-extrabold">{value}</div>{subtitle&&<div className="mt-1 text-xs" style={{color:'var(--muted)'}}>{subtitle}</div>}</div><div className="rounded-xl p-2" style={{background:'color-mix(in srgb,var(--accent) 14%,transparent)',color:'var(--accent)'}}><Icon size={22}/></div></div></div>}
