# Backup và Restore

Backup dùng API backup của SQLite, tạo file `.vdmsp-backup.sqlite` và metadata sidecar. Restore có preview, chế độ merge hoặc replace và copy các cột tương thích giữa phiên bản. Media lớn không nằm trong backup mặc định.
